# Claude Code skills — uBix Core

Version-controlled skills auto-discovered from `.claude/skills/*/SKILL.md`. This file is the human catalog.

| Skill | Use it when |
|---|---|
| [`neptune-sync`](neptune-sync/SKILL.md) | Pulling framework / CLI / MCR / standards / architecture advances from `~/git/project-neptune` into ubixcore without porting neptune's apps — the scope rules, rename table, do-not-rename list, diff → classify → port → verify → land procedure. |

Add a skill: `.claude/skills/<kebab-name>/SKILL.md` with `name` + trigger-rich `description` frontmatter, then a row here.
