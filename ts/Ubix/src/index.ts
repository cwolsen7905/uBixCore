// @ubixsys/ubixcore — framework-level React primitives.
//
// Status: placeholder. Nothing is exported yet; the first shared components land
// as host products factor their UI out. The package exists now so the publish path
// (GitLab npm registry, later npmjs.com) stays proven while there is nothing to lose.
//
// Everything exported from here must pass the boundary test in CLAUDE.md:
//
//   Would a completely unrelated product built on uBixCore -- not a membership
//   platform, not Christian, not creator-facing -- want this code verbatim?
//
// If explaining it requires a host-product noun, it belongs in the host repo.

export {};
