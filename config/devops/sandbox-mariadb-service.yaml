apiVersion: v1
kind: Service
metadata:
  name: mariadb-localhost
  namespace: mariadb
spec:
  type: NodePort
  selector:
    app: mariadb
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30306  # Choose a port between 30000-32767
